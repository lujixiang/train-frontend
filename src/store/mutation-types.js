const key = '@ROOT-'
const mt = {
  GLOBAL_POPUP_LAYER: `${key}/GLOBAL_POPUP_LAYER`,
  // 夜间提示是否提示过了
  IS_MIDNIGHT_NOTICED: `${key}/IS_MIDNIGHT_NOTICED`,
  // 当前时间是否是夜间
  IS_MIDNIGHT: `${key}/IS_MIDNIGHT`
}

module.exports = mt
